import { createGlobalStyle } from 'styled-components';

const ComponenteConteiner = createGlobalStyle`
  body {
    background-color: black; /* Substitua 'cor_que_desejar' pela cor desejada */
    margin: 0;
    padding: 0;
  }
`;

export default ComponenteConteiner;